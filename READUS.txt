        code/
        │── app/
        │   ├── __init__.py
        │   ├── main.py            # file chạy app
        │   ├── core/              # config, kết nối DB
        │   │   ├── __init__.py
        │   │   └── database.py
        │   ├── models/            # ORM models
        │   │   ├── __init__.py
        │   │   └── user.py
        │   ├── repositories/      # thao tác DB (CRUD)
        │   │   ├── __init__.py
        │   │   └── user_repository.py
        │   ├── services/          # logic nghiệp vụ
        │   │   ├── __init__.py
        │   │   └── auth_service.py
        │   ├── routers/           # route FastAPI
        │   │   ├── __init__.py
        │   │   └── auth_router.py
        │   └── templates/         # Jinja2 templates (FE)
        │   |    ├── login.html
        │   |    └── home.html
        │   └── static/              # chứa file tĩnh
        │       ├── images/
        │       │   ├── logo.png
        │       │   └── banner.jpg
        │       └── icons/
        │           └── favicon.ico
        ├── requirements.txt
        └── alembic/               # (tùy chọn) migration DB
pip install -r requirements.txt



[Browser] -- nhập /login --> [FastAPI Router] -- render login.html
[Browser nhập form] -- POST /login --> [FastAPI Router] 
        --> [Auth Service] --> [User Repository] --> [DB MySQL] 
        --> nếu đúng user => Redirect /home 
        --> nếu sai => render lại login.html với error
chạy file
 uvicorn app.main:app --reload


router chịu trách nhiệm nhận request / trả response. (servlet)

service xử lý logic. (controller)

repository làm việc với DB. (DAO)

cách chuyển file 20s sang file có thể xem đc trên web
Mở thư mục chứa video (Shift + Chuột phải → Open PowerShell window here), rồi chạy:
# Tạo thư mục mới tên là converted (nếu chưa có)
$newFolder = "converted"
if (!(Test-Path $newFolder)) {
    New-Item -ItemType Directory -Path $newFolder | Out-Null
}

# Duyệt qua toàn bộ file .mp4 và convert sang thư mục mới
foreach ($file in Get-ChildItem *.mp4) {
    $name = [System.IO.Path]::GetFileNameWithoutExtension($file)
    $outPath = Join-Path $newFolder "$name`_h264.mp4"
    ffmpeg -i $file.FullName -c:v libx264 -crf 23 -preset fast -movflags +faststart $outPath
}

chạy các file train
python continue_train_image.py --model_path "models/image/20251025_152553_Xception/model_final.h5" --n_samples 100 --sampling_mode newest --train_ratio 0.8 --depth normal
python train_image.py --dataset_root "dataset/image" --n_samples 500 --sampling_mode "random" --depth "normal" --out_root "models/image" --train_ratio 0.8
chạy video với bắt buộc cắt lại frame
python train_video.py --dataset_root dataset/video --n_samples 1000 --sampling_mode newest --depth deep --out_root "models/video" --train_ratio 0.75 --pooling_strategy median --force_extract
📊 Tính năng mới được thêm:
✅ Giữ lại frames: Tự động phát hiện và sử dụng frames đã extract trước đó

✅ Video-level evaluation: Đánh giá ở cả mức frame và video

✅ Multiple pooling strategies: 5 phương pháp pooling khác nhau

✅ Visualization: Biểu đồ predictions cho từng video

✅ Comprehensive metrics: Metrics đầy đủ cho cả frame và video level

✅ Flexible CLI: Nhiều tùy chọn command line
python continue_train_video.py --model_path "models/video/20241215_143045_Xception_video/model_final.h5" --n_samples 500 --sampling_mode random --depth normal --train_ratio 0.8
python continue_train_video.py --model_path models/video/20241201_143000_Xception_video/model_final.h5 --n_samples 500 --sampling_mode random --depth deep --train_ratio 0.8 --force_extract --pooling_strategy max